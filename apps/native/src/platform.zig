pub const bundle_id = "app.howler.mac";
pub const display_name = "Howler";
pub const application_support_directory = "Howler";
pub const minimum_macos_version = "13.0";

pub const global_shortcut = .{
    .key = "h",
    .primary = true,
    .alt = true,
};
